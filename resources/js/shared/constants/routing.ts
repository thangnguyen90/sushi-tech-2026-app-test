export const PAGE_NAME = {
    HOME: 'Home',
} as const;

export const PAGE_PATH = {
    HOME: '/',
} as const;

export const PAGE_TITLE = {
    HOME: 'Home',
} as const;

export const ROUTE = {
    NAME: PAGE_NAME,
    PATH: PAGE_PATH,
    TITLE: PAGE_TITLE,
}
